include <stdio.h>

fn main() {
    let foo: int
    let ret: string = ""
    scanf("%d", &foo)

    match foo {
        0 => ret += "zero",
        -2_140_000 .. 0 => ret += "minus",
        1 .. 2_140_000 => ret += "natural"
    }

    println "{ret}"
}